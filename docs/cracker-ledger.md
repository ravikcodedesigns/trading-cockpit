# Cracker — Signal & Study Ledger

> Append-only. Every study/factor run gets an entry: what was asked, how, what came back, and the decision. Format per CRACKER_PLAN.md §3.

---

## 2026-07-06 · P0.1 — Spine correctness fixes

- **Question:** is the measurement instrument (level-memory + swings + replay) trustworthy?
- **Method:** four planned fixes (Beta-posterior retirement, confirmTs, idempotency, rehydration) + a 15-check acceptance suite (`cracker_p01_accept.ts`): unit tests + two fresh 5-day replays compared byte-for-byte + same-day re-run.
- **Result:** 15/15 PASS after three additional bugs the suite itself caught: (1) replay nondeterminism from `ORDER BY ts` tie-breaking; (2) the naive canonical re-sort corrupted the book (depth updates are order-dependent within a ms — absolute sizes); (3) re-run days inherited their own levels from 09:30 (pre-confirmation). Fixed via `file_row_number` capture-sequence ordering, own-day level-birth deletes, tail-only guard.
- **Also learned:** cross-session level memory did NOT exist in the original P1 replay (fresh registry every day). It does now. 05-04/05-07 are thin partial-capture days (legitimately 0 obs).
- **Decision:** instrument certified for Phase 0.2+. Suite is a permanent regression gate. Commit `9d9d38e`.

## 2026-07-06 · P0.2 — Clock alignment, CQG(L2) vs Bookmap(L3)

- **Question:** do the two feeds' timestamps agree well enough for cross-feed joins, and by how much do they differ?
- **Method:** `cracker_p02_clock.ts` — same instrument on both sides (CQG "NQ"=MNQ vs Bookmap MNQ; MES as replication). RTH volume per 50ms bin, Pearson cross-correlation at lags ±3s, parabolic sub-bin refinement, AM/PM split for intra-day drift. Sign: positive = Bookmap late vs CQG.
- **Result:** typical offset **−5 to −50ms** (Bookmap stamps earlier), sharp peaks (peakR 0.2–0.5 vs runner-up ~0.06), MES replicates MNQ. Offset is **day-varying, not constant**: two clean days at **+90ms** (06-04, 06-08 — capture restart signature). **Junk cluster 06-24/06-25/06-26**: peakR ≈ 0.03–0.12 on BOTH symbols — feeds don't correlate at any lag; those days cannot be clock-aligned (timing matches the CL/GC wiring window). Drift flags fired almost exclusively on junk-fit days; one real AM anomaly (MES 07-02). Full per-day table: `docs/cracker-clock-offsets.json`.
- **Decision:** (1) use **per-day offsets** (never a global constant); (2) **quality gate** for any cross-feed join/study: peakR ≥ 0.15 AND intra-day drift ≤ 250ms — else the day is ineligible; (3) **06-24/25/26 excluded** from Phase 0.3/0.4 calibration studies (both symbols); (4) at our research horizons (≥1 min) the residual ≤50ms error is negligible; trade-level matching in 0.3 uses the per-day offset ± a 250ms tolerance window. STOP-flag rule per plan was triggered by junk-fit days — remedied by the quality gate rather than abandoning fine joins wholesale.

## 2026-07-06 · P0.3 — Aggressor calibration: inferred vs true

- **Question:** can L2 flow features (delta/imbalance/absorption) be trusted — i.e., how good is our aggressor classification vs Bookmap L3 ground truth (`is_bid_aggressor` on 5.1M trades/day)?
- **Method:** `cracker_p03_aggressor.ts`, two parts. Part 1 (method error, no cross-feed matching): replay the L3 book on 5 evenly-spaced usable days, classify each trade book-relatively, compare to the true flag; convention pinned empirically. Part 2 (end-to-end): per-minute signed delta from the actual L2 pipeline vs true L3 delta on all 17 usable days (per-day clock offsets applied); CQG's native flag graded alongside.
- **Result:**
  - Convention pinned: `is_bid_aggressor = true ⇔ aggressive BUY` (95.2% day-1 book agreement; matches the empirical note already in `cvd-session.ts:7`).
  - **Book-relative inference: FAILS the frozen bar.** Trade-level agreement 94.5% (✓ ≥90%) BUT degrades hard with size — 1-lot 94.9%, 5–9 73.5%, **10+ 64.1%** (sweeps execute through multiple prices) — and 28.8% of trades print inside the spread (unclassifiable → |vol| ratio ~0.85). Per-minute delta r = **0.910** mean, min 0.670 (worst on trend days) < 0.95 bar.
  - **CQG's NATIVE flag: r = −0.994 vs truth on every one of 17 days** (−0.990..−0.997) — i.e. |r| ≈ 0.994 with the true⇔BUY convention. The **"~3.5×-off CQG flag" folklore is FALSIFIED** — the flag is near-perfect; the folklore almost certainly came from reading it with the inverted sign. (Ironic: footprint.ts was built book-relative specifically to avoid this flag.)
  - **Live-code audit:** cvd-session / order-book / backtest-expl already use true⇔BUY correctly. **`morning-brief.ts:85` had it inverted** (buy/sell vols swapped in the brief) — fixed this commit.
  - Data flag: 3 of 5 Part-1 days (06-15, 06-19, 07-03) could not sustain a valid L3 book (depth present, book never two-sided) — does not affect this verdict; logged as an open item for Phase 4b (L3 institutional work).
- **Decision:** **L2 flow features CERTIFIED for the full 54-day set — via the NATIVE flag (true⇔BUY), not book-relative inference.** Footprint/Tier-2 wiring (Phase 1.5) switches its aggressor source to the flag; book-relative stays as a fallback only. Error bars: per-minute delta |r| ≈ 0.994. Book-relative method verdict stands as FAIL — do not use it where the flag exists.

## 2026-07-06 · P0.4 — Micro-vs-mini footprint agreement (the routing study)

- **Question:** we screen on micro (54-day power) and confirm on mini (institutional truth) — do the two crowds' footprints agree well enough for that to work, per feature family?
- **Method:** `cracker_p04_micromini.ts` — mini vs micro compared WITHIN the same Bookmap capture (same clock; isolates crowd difference from feed artifacts), native flag both sides, 13 clean days each pair. Metrics: per-minute delta r, per-bin profile correlation + POC distance + 70% value-area Jaccard, per-bin imbalance-category Cohen's κ. Estimation study — distributions, no pass/fail.
- **Result (NQ/MNQ · ES/MES):**
  - **Profile family: near-identical.** vol-r 0.966 · 0.956; VA-Jaccard 0.88 both; median |POC dist| 4.0pt · 0.3pt. Caveat: NQ shows **twin-peak instability** — ~5/13 days have POC distance 50–120pt (two competing HVNs; the crowds pick different peaks) while value areas still overlap ≥0.69 → use **HVN sets, not single POC**, as level sources.
  - **Delta family: correlated but attenuated.** r1m 0.799 ± 0.029 (NQ — remarkably stable) · 0.645 ± 0.115 (ES). The crowds genuinely differ minute-to-minute.
  - **Imbalance family: does NOT transfer.** κ 0.162 · 0.267 — bin-level imbalance flags are crowd-specific.
- **Decision (routing table):**
  | family | route |
  |---|---|
  | volume structure (POC/HVN/LVN/VA/zones) | **micro-OK** — screen on the full 54-day L2 set (HVN-set caveat) |
  | delta/flow | **BOTH-with-correction** — screen on micro, expect ~0.8 (NQ) / ~0.65 (ES) attenuation vs institutional truth; mini confirmation mandatory; **Phase 2.3 power table must incorporate the attenuation** (a true mini effect appears shrunk on micro) |
  | bin-level imbalances (incl. stacked) | **mini-ONLY** (L3, ~13–17 days, mechanism-grade); revisit coarser-granularity definitions if imbalance factors matter later |
- Composition with P0.3: L2-micro ≈ L3-micro at 0.994 (feed error is negligible); L3-micro ≈ L3-mini at ~0.8 (crowd gap is the dominant term). The pipeline's total distortion budget is now measured end-to-end.
- **Post-study simplification (user decision):** the routing-with-correction model is replaced by the two-line DATA POLICY (CRACKER_PLAN §1.5): L2 full history = levels & structure ONLY; L3 mini NQ = flow discovery; ES mini = replication; lockbox = next 10 forward days. No renaming of the L2 "NQ"/"ES" labels (they mean MNQ/MES — documented, not refactored).

## 2026-07-06 · P0.5 — Estimator freezes (σ_ev + size-aware imbalance z)

- **Question:** freeze the two statistical rulers before any research uses them.
- **σ_ev** (`src/l3/sigma-ev.ts`, frozen): EWMA variance of drift-stripped 1-min log returns, half-life 30 min, session-anchored (18:00 ET reset, warmup carries prior session's value), floor 0.5pt / cap 60pt, output in points per √min + σ√h horizon scaling. Units: floors the Gate-2 stop, sizes the vertical barrier, feeds the swing detector.
- **Size-aware imbalance z** (`footprint.ts`, frozen): null model = each TRADE (not contract) is a fair coin → Var(buyVol−sellVol) = Σsize², z = (a−b)/√(Σs²). Reduces exactly to the old binomial with 1-lots; a lone 36-lot block at z_naive≈5.06 now reads z≈0.89 → no fake significance from single blocks.
- **Acceptance (8/8):** synthetic-vol recovery within 2% (est 20.6 vs true 21.0pt); drift-stripping verified (10bp/min trend + 2bp noise reads 5.5pt ≈ noise, not 27pt drift); session-reset carry; floor; binomial-reduction; block-rejection; reference days sane (trend 06-05 σ 16.4pt > chop 05-29 10.7pt > normal 06-02 7.1pt).
- **Decision:** both estimators FROZEN. **PHASE 0 COMPLETE** — instrument hardened (0.1), clocks measured (0.2), aggressor certified via native flag (0.3), crowd-agreement routed → simplified to the data policy (0.4), rulers frozen (0.5). Next: Phase 1 (the trace).

## 2026-07-06 · P1 build → DATA-INTEGRITY FINDING: mbo-parquet capture order destroyed from ~06-19

- **What happened:** the full trace build produced visits only on 06-16→06-18; every later day = 0 observations (book permanently crossed, e.g. 66,377 crossed checks on 06-23 with both sides populated).
- **Diagnosis chain:** sides correct → deletions present → sanity filters remove only ~180/25M rows → **monotonicity check is the smoking gun**: on working days ts_ms is perfectly monotonic in file order (0 backward steps); on failing days **94–99% of rows sit >5s behind the running max, worst offset exactly 24h** — i.e. `file_row_number` no longer reflects capture order AT ALL. The files were rewritten/shuffled after conversion (prime suspect: the nightly parquet-compaction job; converter multi-pass writes possible too). Same-ms absolute-size depth updates replayed out of order → permanently poisoned book (the exact mechanism P0.1 identified on L2, here at full scale). Garbage contract labels (NQU6BMD, NQU6NQU6NQU6…, ~5–10 rows/day) additionally indicate converter line-tearing.
- **This retroactively explains:** P0.3 Part-1 zero days (06-15/06-19/07-03 — open item CLOSED), and plausibly the P0.2 junk cluster 06-24/25/26 (if ts VALUES are also wrong on some rows, cross-feed volume bins smear; re-run P0.2 after repair).
- **Not affected:** trades-table GROUP-BY aggregations (order-independent) — P0.3 Part-2 (per-minute deltas) and P0.4 conclusions stand. The trace ENGINE is proven working (439 visits on 06-17/18, placebos live, outcomes resolved).
- **Recovery:** raw BMD logs are INTACT for all affected days (06-19→07-06, mini + micro). Remediation plan (pending user go): (1) inspect converter + compaction for the reorder; (2) add an explicit `seq` column (log line number) so capture order survives any rewrite; (3) re-convert affected days for NQ/MNQ/ES/MES; (4) acceptance = monotonicity check + trace rebuild + P0.2 re-run on the junk cluster.

## 2026-07-06 · Capture-order repair — EXECUTED & VERIFIED (minis)

- **Root cause pinned:** `dedup_parquet_store.py` (one script, ALL symbols/tables) rewrote partitions via `SELECT DISTINCT` with a deliberate no-ORDER-BY (perf comment in code). Fixes shipped: `seq` INT64 = source-log byte offset on all three schemas (deterministic across re-reads → at-least-once dups still collapse); dedup rewrites now `ORDER BY (ts_ms, seq)` and REFUSE legacy no-seq partitions; nightly compaction DISARMED pre-03:10; live tail restarted 22:04 ET with seq (all future capture order-proof).
- **Re-conversion (minis):** NQU6+ESU6 logs 06-19→07-05 (~107 GB) → staging → **verification 78/78 partitions PASS** (zero displaced rows per source file; the initial 07-02 "failure" was a verifier artifact — midnight-boundary spillover puts two logs in one date partition and their byte-offset seqs collide; replays sort (ts_ms, seq) and are immune; verifier now partitions by filename). Swapped into the live store; originals quarantined (`.corrupt-order-quarantine`, 4.3 GB).
- **BONUS FINDING — the old dedup also ATE real trades:** staging carries **+1–5% more trades** than the original store (NQ ~+1.2%, ES ~+3–5%): full-row DISTINCT merged legitimate identical trades (two 1-lot fills, same ms/price/flags). The code's "verified safe for trades" claim was false. All trades-based historical numbers were slightly undercounted; seq makes rows unique and recovers them.
- **Trace rebuild = repair PROVEN:** 439 visits/2 days → **4,312 visits/12 clean days** (06-19 97, 06-22 368, 06-23 262, 06-24 349, 06-25 628, 06-26 383, 06-30 335, 07-01 535, 07-02 826, 07-03 90). Remaining zeros = Sundays + thin 06-16 + live 07-06 (re-converts after 07-07 close).
- **Baseline at scale (the wall P3 must beat):** hold rates — swing 0.665, placebo-random 0.654, placebo-shifted 0.661, round 0.669. **Raw hold-rate carries ZERO level-identity information.**
- Pending: micro re-conversion (in flight) → swap → P0.2 clock-study re-run on 06-24/25/26 → prove order-safe dedup on one day → re-enable compaction. CL/GC = BACKLOG item 16.

## 2026-07-07 · Repair completed: micros swapped, dedup proven order-safe, compaction re-enabled — and one hypothesis FALSIFIED

- **Micros re-converted & swapped:** MNQU6+MESU6 (~220 GB) → all partitions PASS (zero displaced per source file; trades +1.7–2.6% recovered) → 80 partitions swapped; quarantine now 12 GB total (160 partitions preserved).
- **HYPOTHESIS FALSIFIED — the P0.2 junk cluster was NOT the shuffle.** Re-run on repaired data: 06-24/25/26 remain uncorrelatable (peakR 0.03–0.10). Correct in hindsight — P0.2 used order-independent GROUP-BY binning, so row order never touched it. Those days have a genuine timestamp-VALUE problem on the Bookmap side (window coincides with the addon exchange-time migration, BACKLOG #13). Their exclusion from fine cross-feed joins stands, now with the right cause attached. All other days: offsets stable (MES σ 24ms).
- **Order-safe dedup PROVEN on live data:** ran on all 18 of 07-07's partitions — compacted outputs ts-monotone in file order (12/12 checked PASS); the 3.1% removed = true at-least-once duplicates from the 22:04 converter restart (same line ⇒ same seq ⇒ collapsed), while legitimate identical trades survive (different seq). Both dedup behaviors correct simultaneously.
- **Nightly compaction RE-ENABLED** (order-safe; refuses legacy no-seq partitions). Remaining tail: re-convert 07-06 after today's close (its partition is mixed pre/post-fix); CL/GC = BACKLOG 16.
- **Net state:** the L3 store is order-proof end-to-end — capture (raw logs) → conversion (seq) → compaction (ORDER BY + seq-gate) → verification tooling in repo. Discovery dataset: 12 clean days + every day forward.

## 2026-07-07 · P1.6 — Kernel volume profile: HVN/LVN sources, confluence, structural 1R (first primitive rebuilt under the rebuild directive)

- **Question:** complete Phase 1.6 — Tier-1 volume levels (kernel-smoothed profile → POC/HVN/LVN as level sources), confluence score, and the FINAL frozen 1R stop (LVN-structural term replacing the interim σ-only rule).
- **Method:** new Cracker-owned module `src/l3/volume-profile.ts` (built from first principles, not a copy-edit of legacy code):
  - Weighted Gaussian KDE of volume-at-price on the exact tick histogram. Bandwidth = **Improved Sheather-Jones** (Botev–Grotowski–Kroese 2010, DCT fixed-point) with **Kish n_eff** = (Σs)²/Σs² as the sample size (blocky prints widen h — same principle as the frozen P0.5 imbalance z); weighted-Silverman fallback, method always reported.
  - **Structural bandwidth floor 2.0pt (NQ)**: probed 3 reference days — raw ISJ (h≈0.6pt, MISE-optimal at n_eff~2·10⁵) fragments single shelves into 13–16 "HVNs" (six inside one 60pt shelf); the plan-frozen ≈2pt scale yields 7–12 cleanly separated shelves; 4pt merges real ones. h = max(ISJ, 2pt) — selector still adapts UPWARD on thin sessions. Floor is per-instrument (ES value set at Phase 1.7).
  - HVN/LVN = topographic-**prominence**-qualified extrema (≥10% of peak density; edge-artifact zone excluded); HVN SET kept per the P0.4 twin-peak caveat, POC = global max, first of the set. Value area 70% computed as descriptive convention only.
  - Trace wiring: prior-VALID-session profile (walk-back ≥50k contracts RTH — Monday reads Friday, thin 06-19-style holidays skipped) → `hvn`/`lvn` level sources, causal by construction. `confluence_n` = distinct structural sources among OTHER active levels within ±5pt at visit OPEN (self excluded → placebos measured with the same yardstick; placebos never contribute, `round` does). **1R finalized per direction**: max(1 tick beyond nearest prior-session LVN behind the level within 5×σfloor, σ_ev·√15); vertical barrier T = 2·(1R/σ_1m)² min capped 60 (floor-1R reproduces the interim 30min exactly); `stop_src_l/s` records which term bound. Polarity history = derivable from the interactions `side` sequence (no schema change). Trace schema v3 (regenerable; full rebuild).
- **Acceptance:** `cracker_p16_accept.ts` **24/24 PASS** — synthetic ground-truth recovery (bimodal/trimodal modes tick-exact, valley LVNs, no spurious extrema), determinism, ISJ<Silverman on multimodal, Kish monotonicity on thin samples (h 4.18→8.77pt as n_eff 745→62), clamp/degenerate guards, structural-stop geometry (5 cases), 3 real-day structure sanity. **P0.1 regression gate re-run after the src/l3 edits: 15/15 PASS**, twin 5-day replays byte-identical.
- **Result (full rebuild, 12 clean days):** 5,200 visits (was 4,312; +888 = the new hvn/lvn levels). Hold rates: swing 0.665 (baseline REPRODUCED exactly), placebo-random 0.654, placebo-shifted 0.661, round 0.669, **hvn 0.668, lvn 0.679** — raw hold rate at volume levels sits in the placebo band too, extending the P1 conclusion: NO level source carries identity in raw hold rate; discrimination must come from Phase-2 features. Structural stop engaged on 17% of visits (avg 1R 105pt vs 74.8pt floor). Confluence distribution 0:2101 · 1:2140 · 2:782 · 3:157 · 4:20. Days 07-05/06/07 = 0 obs (Sunday + pending re-conversion + live day), expected.
- **Decision:** Phase 1.6 COMPLETE; volume-profile module certified as Cracker instrument code. Outcome columns now carry the FINAL 1R definition — Phase-2 factor tests consume these, never the interim ones. Remaining Phase 1: 1.4 context columns, 1.7 ES trace. Deferred, pre-registered as ideas only: shifted-HVN placebo if volume-level factors reach Phase 3; developing (intraday) profile as an alternative LVN basis.

## 2026-07-07 · P1.4 — Context columns (tod phase · NQ–ES common factor · morning IV)

- **Question:** complete Phase 1.4 — the stratification columns the plan pre-registered: time-of-day phase, minimal NQ–ES common-factor state, expected-range conditioning (the validated IV→range forecaster input). Day dir-ratio already existed in `day_context`.
- **Method:** pure functions in `trace.ts` (`todPhase`, `commonFactor`) + idempotent `resolveContext` pass → new `visit_context` table + `morning_iv` on `day_context`. NO book replay needed — the pass joins existing visit rows against 1-min closes of OUR two feeds, so it ran as a backfill (`cracker_p14_context.ts`) over the standing 12-day trace; the runner also executes it for every future day. Frozen definitions:
  - `tod_phase`: open [09:30,10:30) · close ≥14:30 · mid between (ET).
  - `es_agree`: fraction of the 6 non-overlapping trailing 5-min log returns where sign(NQ)=sign(ES), pairs with a zero return excluded, null if <4 valid pairs.
  - `rs_30m_bp`: 30-min log-return differential (NQ−ES)×10⁴.
  - `morning_iv`: mean NDX ATM-IV 09:30–10:00 ET from the quantdata store (cache-first, fetch-and-persist on miss). Stored RAW — conditioning is rank-based; fitting an IV→points calibration would add an estimated parameter for nothing.
  - **Knowledge times documented per column** (close-ts / close-ts / 10:00 ET / 16:00 for dir_ratio): these are STRATIFICATION columns, not entry features; Phase-2+ tests must respect the stamps.
- **Acceptance:** 12/12 PASS — units (phase boundaries; agreement 1/0 on co-/anti-moving synthetic tapes; rs exact to 1e-9; <4-pairs→null; **causality: appending future bars leaves the window unchanged**; zero-return exclusion) + integration (one context row per visit 5,200/5,200; idempotent re-run; es_agree avg 0.877 null 0%; rs avg −8.8bp null 0%; phases open 1,543 / mid 2,778 / close 879; morning_iv 11/13 days, IV 29.9–54.1%).
- **Two nulls chased to ground, not waved off:** 06-19 = Juneteenth (options closed — correct null); 07-03 = vendor 422 "data unavailable" for the half-session (verified live). 07-06 was initially missed because the backfill looped visit_features days only — a 0-visit day still owns a day_context row; fixed (union with day_context) and its IV fetched (405 buckets).
- **Decision:** Phase 1.4 COMPLETE. Remaining Phase 1: 1.7 ES trace. es_agree averaging 0.877 confirms the two feeds are measuring the same market (a sanity mirror of P0.4's delta-family r); rs_30m and phase splits now available to every Phase-2 factor test.

## 2026-07-07 · P1.7 — ES trace switched on (same code, ES config) — PHASE 1 COMPLETE

- **Question:** run the identical spine+trace pipeline on ES mini so the replication set accumulates from day one (analysis deferred, per plan).
- **Method:** per-instrument config (`INSTR` in cracker_p1_trace.ts, `TRACE_SYM=ES`). Dimensionless constants shared (σ multipliers, Beta prior, retirement, hysteresis, z cutoffs). Price-dimension constants scaled by the NQ/ES price ratio (~4.0 at 30,000/7,500), tick-friendly: merge/confluence 5→1.25pt, tape window 4→1pt, cluster 10→2.5pt, imbalance bin 1→0.25pt, swing δ-caps [40,90,180]→[10,22.5,45]pt, base vol floor/cap 2/40→0.5/10pt, σ cap 60→15pt (σ floor stays 0.5pt = 2 ticks on both — the dead-tape guard is tick-scale). Round grid: 10s with 50-majors (ES flow convention; NQ 50s/100s); shifted-placebo offset scales with the grid. **Profile floor probed on 3 real ES days with the same criterion as NQ**: 0.5pt fragments one shelf into three "HVNs" 4–8pt apart, 1.0pt yields 5–7 clean shelves, 2pt merges → **ES H_FLOOR_PTS = 1.0**. Engines gained per-instance cfg where missing (SigmaEv floor/cap, LevelMemory). Context columns: the common factor stays fixed as (NQ, ES) market-state — es_agree symmetric, rs always NQ−ES, morning_iv = NDX for both (market-vol proxy; SPX drift has ~10 days coverage, not viable). Safety: fresh-wipe now fires only on the default NQ run; a non-NQ run can never delete the shared DB.
- **Result:** ES trace live — **4,174 visits / 13 days (06-17→07-06)** in the same cracker-trace.db. Hold rates: swing 0.643 · placebo-random 0.654 · placebo-shifted 0.668 · round 0.664 · hvn 0.631 · lvn 0.651 — the placebo-band conclusion REPLICATES on ES (no source separates in raw hold rate). Structural stop engaged 30% (avg 1R 18pt vs 10.5pt floor — implied σ_1m ≈ 2.7pt, sane). Context: es_agree 0.878 (mirrors the NQ-side 0.877 — symmetric, as it must be), rs −8.3bp, morning_iv 11/13. **NQ rows verified untouched** (5,200/5,200/5,200/5,200 + 487 levels, exact snapshot match).
- **Decision:** **PHASE 1 COMPLETE** (1.1 outcomes ✓ 1.2 uniqueness/clusters ✓ 1.3 placebos ✓ 1.4 context ✓ 1.5 footprint phases ✓ 1.6 volume levels + structural 1R ✓ 1.7 ES ✓). Both traces accrue forward daily. Next: **PHASE 2** — the measurement→hypothesis gate: feature distributions, per-factor power table (with the P0.4 micro-attenuation), and the formal null from the placebo wall (NQ 0.654–0.669 / ES 0.643–0.668).

## 2026-07-07 · P2 — Nulls & power: gate PASSED, one premise FALSIFIED, power table published

- **Question:** calibrate the statistics before factor hunting — (2.1) does the pipeline read zero correctly at placebo levels; (2.2) publish the baseline curves; (2.3) publish the MDE power table.
- **2.1A markout self-test: PASS** (pre-registered gate unchanged). Drift-adjusted weighted mean markout at placebo-random ≈ 0: 8/8 cells within 99% CI, 7/8 within 95% (ES 30m marginal at +1.16pt, allowed by the frozen ≥7/8 rule). n=674–791 per cell.
- **2.1B barrier self-test: the pre-registered premise was FALSIFIED — twice, instructively (both runs preserved):**
  - v1 (iid Gaussian sim, constant day drift): FAIL 12/16 — NQ shorts 1–2R overpredicted (drift is bursty, not uniform), 3R hits underpredicted both symbols (fat tails).
  - v2 (empirical paired null — identical march at 50 seeded random minutes ±60min on the same day's REAL path): FAIL 13/16, now with structure: **NQ longs +6pp at 1.5–2R beyond the null @99.5%**.
  - **Diagnosis:** placebo LEVELS are information-free, but visit-CLOSE moments are state-selected — price just departed a consolidation decisively — and carry real short-horizon continuation shared by ALL visits regardless of level identity. No bystander null (analytic or random-time) can predict state-selected entries; the plan's "placebo = theory" premise was wrong, and the deviation is a market measurement, not a pipeline bug.
  - **Gate revised (documented, not shopped):** 2.1B now gates what it can honestly gate — bookkeeping. Every stored barrier label independently re-derived from raw bars: **12,040/12,040 labels identical, 0 entry_px mismatches → PASS.**
- **2.2 baselines published:** (a) hold-rate CIs per source/symbol (all sources overlap the placebo band on both symbols — the wall, now with error bars; tightest: NQ swing 0.665 [0.648, 0.689]); (b) random-time markout yardstick (NQ sd 17.7/41/70/94pt at 1/5/15/30m; ES 2.6/6.4/10.5/14.0pt); (c) **NEW — the visit-mechanics continuation curve**: placebo-visit W-rate minus paired random-time null = NQ long +5.7/+6.7/+6.0/+2.2pp at 1/1.5/2/3R (shorts −7.0/−3.6/−2.9/−1.5pp), ES ≈ flat (±2pp). **Binding consequence for Phase 3: factors are tested VISITS-vs-VISITS (factor bucket vs placebo/other visits) — never vs random-time or analytic nulls, which would flatter every factor by up to ~6pp on NQ.**
- **2.3 power table published → `docs/cracker-power.md`** (re-runnable, stamped, MDE = 2.8×day-block-bootstrap SE). Headlines at full n: NQ 1.15pt@1m / 3.84pt@15m / 7.90pt@30m; ES 0.30pt@1m / 0.85pt@15m. Sobering small-n reality: a 200-visit NQ bucket can only prove ≥10.6pt@1m (day-cluster variance dominates at 12 days) — most fine-sliced factor buckets are PRE-DECLARED untestable until forward days accrue. Micro-screen columns include the P0.4 attenuation (÷0.8 NQ, ÷0.65 ES).
- **Decision: GATE 2→3 OPEN** — measurement code certified (markouts + label bookkeeping), baselines + continuation curve published, power table live. Phase 3 (single-factor ladder) may begin, with the visits-vs-visits control design and the MDE testability rule binding.

## 2026-07-07 · P2 — Nulls & power: gate PASSED, one premise FALSIFIED, power table published

- **Question:** calibrate the statistics before factor hunting — (2.1) does the pipeline read zero correctly at placebo levels; (2.2) publish the baseline curves; (2.3) publish the MDE power table.
- **2.1A markout self-test: PASS** (pre-registered gate unchanged). Drift-adjusted weighted mean markout at placebo-random ≈ 0: 8/8 cells within 99% CI, 7/8 within 95% (ES 30m marginal at +1.16pt, allowed by the frozen ≥7/8 rule). n=674–791 per cell.
- **2.1B barrier self-test: the pre-registered premise was FALSIFIED — twice, instructively (both runs preserved):**
  - v1 (iid Gaussian sim, constant day drift): FAIL 12/16 — NQ shorts 1–2R overpredicted (drift is bursty, not uniform), 3R hits underpredicted both symbols (fat tails).
  - v2 (empirical paired null — identical march at 50 seeded random minutes ±60min on the same day's REAL path): FAIL 13/16, now with structure: **NQ longs +6pp at 1.5–2R beyond the null @99.5%**.
  - **Diagnosis:** placebo LEVELS are information-free, but visit-CLOSE moments are state-selected — price just departed a consolidation decisively — and carry real short-horizon continuation shared by ALL visits regardless of level identity. No bystander null (analytic or random-time) can predict state-selected entries; the plan's "placebo = theory" premise was wrong, and the deviation is a market measurement, not a pipeline bug.
  - **Gate revised (documented, not shopped):** 2.1B now gates what it can honestly gate — bookkeeping. Every stored barrier label independently re-derived from raw bars: **12,040/12,040 labels identical, 0 entry_px mismatches → PASS.**
- **2.2 baselines published:** (a) hold-rate CIs per source/symbol (all sources overlap the placebo band on both symbols — the wall, now with error bars; tightest: NQ swing 0.665 [0.648, 0.689]); (b) random-time markout yardstick (NQ sd 17.7/41/70/94pt at 1/5/15/30m; ES 2.6/6.4/10.5/14.0pt); (c) **NEW — the visit-mechanics continuation curve**: placebo-visit W-rate minus paired random-time null = NQ long +5.7/+6.7/+6.0/+2.2pp at 1/1.5/2/3R (shorts −7.0/−3.6/−2.9/−1.5pp), ES ≈ flat (±2pp). **Binding consequence for Phase 3: factors are tested VISITS-vs-VISITS (factor bucket vs placebo/other visits) — never vs random-time or analytic nulls, which would flatter every factor by up to ~6pp on NQ.**
- **2.3 power table published → `docs/cracker-power.md`** (re-runnable, stamped, MDE = 2.8×day-block-bootstrap SE). Headlines at full n: NQ 1.15pt@1m / 3.84pt@15m / 7.90pt@30m; ES 0.30pt@1m / 0.85pt@15m. Sobering small-n reality: a 200-visit NQ bucket can only prove ≥10.6pt@1m (day-cluster variance dominates at 12 days) — most fine-sliced factor buckets are PRE-DECLARED untestable until forward days accrue. Micro-screen columns include the P0.4 attenuation (÷0.8 NQ, ÷0.65 ES).
- **Decision: GATE 2→3 OPEN** — measurement code certified (markouts + label bookkeeping), baselines + continuation curve published, power table live. Phase 3 (single-factor ladder) may begin, with the visits-vs-visits control design and the MDE testability rule binding.

## 2026-07-07 · P3 — Frozen harness built · F1 (level-source identity): NULL / UNDERPOWERED

- **Harness (`cracker_p3_harness.ts`, frozen):** outcome y(h) = side-signed drift-adjusted markout; uniq_w weighting everywhere; visits-vs-visits contrasts only (P2 rule; placebo pool = random+shifted, round excluded); weighted Spearman IC for continuous factors; tercile boundaries fit on train only; day-block bootstrap B=2000 (shared resamples → paired real-vs-twin diffs); chronological 60/40 train/validation inside the discovery freeze (≤2026-07-07; later days = LOCKBOX, harness refuses to read them until Phase 5); verdicts per declared mechanism horizon — EDGE (train+validation CI95 exclude 0, same sign, beats twin) / UNDERPOWERED (MDE=2.8×SE > E*=0.15×random-time SD) / NULL. STOP/GO per factor; NULLs drop forever.
- **F1 spec (pre-registered):** categorical — source ∈ {swing,hvn,lvn,round} vs placebo pool; declared horizons 5m/15m; hold-rate contrast secondary. Coarse-vs-fine swing split DEFERRED (kind persists only high/low; scale tagging queued for next rebuild).
- **F1 result: no source earns EDGE.**
  - NQ swing (n=1428/1105): the cautionary cell — train Δy(15m) +14.4pt CI [1.3, 36.4] *significant*, validation **−3.4pt CI [−5.4, −0.9] — opposite sign, also significant** → NULL by the same-sign rule. The 7th train-set pattern in project history to die OOS; the harness caught it structurally.
  - NQ hvn: Δy(5m) train +4.3 / valid +4.6 — SAME sign, decent size, neither CI excludes 0; MDE 8.55 > E* 6.15 → **UNDERPOWERED, forward-queue** (the one genuinely promising cell). NQ lvn 5m likewise UNDERPOWERED (train +6.8 sig, valid +1.3 n.s.).
  - ES: all sources NULL at declared horizons except lvn (UNDERPOWERED, n=99 validation). Contrasts tiny (±0.5pt).
  - Δhold ≈ 0 everywhere (secondary) — triple-confirms P1/P2: level identity carries no raw hold-rate information.
- **Reading:** merely BEING at a swing/HVN/LVN/round level predicts nothing detectable at current power. Level identity is context, not signal — discrimination must come from flow/interaction factors (F4–F8) or wait for power (hvn 5m retest as forward days accrue).
- **Decision:** F1 recorded — swing/round NULL at declared horizons (dropped from compositions as standalone factors); hvn/lvn 5m → forward-accumulation queue. Next: F2 (confluence count) through the same harness.

## 2026-07-07 · P3 · F2 — confluence count: NULL (NQ 5m, powered) / UNDERPOWERED elsewhere

- **Spec (pre-registered):** factor = confluence_n (distinct other structural sources ±5pt at visit open); population = real-source visits; SHARP twin = same count on placebo visits (isolates "is being structure special" from "is being near structure special"); tests = weighted Spearman IC + stacked(≥2)-vs-lone(0) contrast; declared 5m/15m.
- **Harness fix mid-factor (disclosed):** IC verdicts had a units bug — the points-denominated E* (6.15pt) made dimensionless ICs always "powered" → auto-NULL. Added frozen PLAUSIBLE_IC ρ* = 0.05 (conventional decision-relevant IC bar; chosen on convention, not tuned — the fix can only relabel NULL→UNDERPOWERED, never produce EDGE). F1 unaffected (its contrasts are in points).
- **Result: no EDGE.**
  - NQ IC(5m): train +0.060 CI [0.023, 0.084] significant; validation +0.034 CI [−0.004, 0.061] — same sign, near-miss, and POWERED (MDE 0.046 ≤ ρ* 0.050) → **NULL by the frozen rule.** Stacked-vs-lone Δ(5m): train +6.1pt CI [3.2, 9.4] sig, valid +7.1pt same-sign n.s., twin +4.0 n.s. — the recurring "NQ 5m structure bounce" theme (F1 hvn showed the same shape) stays sub-threshold.
  - NQ IC(15m): sign flips train/valid, UNDERPOWERED (MDE 0.084). ES 5m/15m: UNDERPOWERED (MDE 0.099/0.148, validation n≈950).
  - Twins ≈ 0 throughout — proximity-to-structure alone does nothing either.
- **Reading:** stacking levels does not measurably improve the bounce at current power; the NQ-5m near-miss is the same sub-MDE theme as F1-hvn and accrues forward with the sample. Confluence as a STANDALONE factor: dropped (NULL at the powered declared cell). Underpowered cells (NQ 15m, ES both) → forward-accumulation queue.
- **Decision:** F2 recorded. Next: F3 (visit-index / test-over-test hold posterior — the corrected 94/7 question) or jump to F4 (absorption) per user's mechanism priority.

## 2026-07-07 · P3 · F3 — visit-index / test-over-test posterior: NULL/UNDERPOWERED — the "94/7" ghost dies OOS

- **Spec (pre-registered):** history = observed prior visits of the same level_id, strictly causal; (a) visit-index IC (all visits); (b) prior-only Beta-posterior IC within the has-history population (survivorship handled by ranking WITHIN has-history, never has-vs-hasn't); (c) test-over-test Δhold = P(hold|prev held) − P(hold|prev broke), secondary. Sharp twin: placebo levels accrue histories through the same registry. Declared 5m/15m, ρ*=0.05.
- **Result: no EDGE anywhere.**
  - (a) visit-index: UNDERPOWERED at all declared cells (MDE 0.058–0.125 vs ρ* 0.05); NQ-5m validation −0.055 sig but train +0.004 — no same-sign support.
  - (b) prior-record posterior: NQ 15m sign-flips (train +0.026 → valid −0.095); ES 5m NULL (powered); ES 15m/30m same-sign positive train/valid (+0.040/+0.045, +0.068/+0.052) but UNDERPOWERED → forward-queue.
  - (c) **the centerpiece: NQ test-over-test Δhold = +6.8pp in train, CI [0.9, 10.7] — SIGNIFICANT in-sample — then −5.9pp in validation.** The exact shape of the original 94/7 claim: in-sample, levels appear to "remember"; out-of-sample the memory reverses. With the twin showing the same reversal, the in-sample effect reads as day-regime artifact, not level memory. ES: nothing in either half.
- **Reading:** a level's own track record does NOT predict its next test at current power — and the historical "levels that held keep holding" folklore is now measured as an in-sample mirage (3rd such train-only pattern this phase: F1 swing-15m, F2 partially, F3 TOT). Distribution note: 80% of real visits are to levels with 3+ prior visits — levels get retested constantly, so if memory existed the sample to see it is there.
- **Decision:** F3 recorded — visit-index and TOT dropped as standalone factors (NULL-pattern with sign flips); ES posterior 15/30m cells → forward-accumulation queue. Ladder next: F4 (absorption ratio at contact) — first flow factor.

## 2026-07-07 · P3 · F4 — absorption ratio: UNDERPOWERED at declared horizons — but a robust INVERTED signal at 1m → F4b pre-registered

- **Spec (pre-registered):** factor = absorb_ratio (P1.5 frozen: contact volume / max(penetration, 1 tick)); mechanism prior "absorption ⇒ defense ⇒ bounce"; declared 5m/15m; sharp placebo twin; non-gating confound check = IC of raw ct_vol.
- **Declared-horizon verdicts (the official F4 result):**
  - NQ 5m: train IC −0.050 CI [−0.096, +0.006] (misses), valid **−0.083 CI [−0.134, −0.043] significant**; tercile Δ valid **−8.4pt CI [−13.5, −3.1]** → same sign, train near-miss → **UNDERPOWERED** (MDE 0.068). NQ 15m: sign instability, UNDERPOWERED. ES: both declared cells UNDERPOWERED (negative lean at 5m, −0.044/−0.045 train/valid, n.s.).
- **The surprise — NQ 1m (UNDECLARED horizon): train IC −0.060 CI [−0.099, −0.009] AND valid −0.059 CI [−0.117, −0.018] — both significant, same sign, twin ≈ 0 (−0.004), tercile −3.1pt/−2.4pt both significant.** Would satisfy every EDGE criterion — at a horizon we didn't declare, with the sign INVERTED vs the declared mechanism: HIGH absorption predicts WORSE bounce (continuation INTO the level) at 1 minute. Confound check clears volume (ct_vol IC ≈ −0.02/0.00 at 1m — it's the ratio, not busy tape). Frozen rules: an undeclared-horizon finding CANNOT be promoted to EDGE from the sample that generated it (that is horizon-shopping).
- **→ F4b PRE-REGISTERED (2026-07-07, before the L2 build completes):** hypothesis "high contact absorption predicts short-horizon (1m) continuation INTO the level (negative IC), visits-vs-visits" — declared horizon **1m**, expected sign **negative**, test set = **L2 screening set restricted to days < 2026-06-16** (zero overlap with the L3 discovery days → truly independent), same harness, twin required. ES directional consistency (−0.036 at 1m) noted as weak support. Mechanism reading if confirmed: heavy volume absorbed per point of give marks a level under sustained attack — the defense is absorbing but the attacker keeps coming (consistent with WHY the DDA hold-side thesis failed; this is a materially different claim — short-horizon markout, not bracket trades — so the DDA settled-null is not relitigated).
- **Decision:** F4 official = UNDERPOWERED at declared horizons (forward-queue); F4b = new registered hypothesis awaiting the independent L2 sample. Next on the ladder: F5 (contact delta sign & magnitude).

## 2026-07-07 · P3 · F5 — contact delta: UNDERPOWERED/≈0 at real levels — while the TWIN shows flow-continuation → F5b pre-registered

- **Spec (pre-registered):** d* = side-signed contact delta (aggression in the bounce direction positive for both sides); variants raw and d*/ct_vol (imbalance fraction); declared 1m/5m; real pool vs sharp twin.
- **Official verdicts: UNDERPOWERED at every declared cell, both symbols, both variants** (MDE 0.074–0.185 vs ρ* 0.05 — delta factors carry heavy day-clustering variance). Point estimates at real levels ≈ 0 (NQ raw 1m: −0.002/+0.002 train/valid) — no evidence that aggressor flow direction AT a real level predicts the resolution.
- **The emergent finding lives in the TWIN: at PLACEBO levels, bounce-directed flow PREDICTS the bounce** — NQ d*/ct_vol twin IC +0.078 CI [0.049, 0.104] @5m, +0.092 [0.035, 0.161] @15m, +0.081 [0.005, 0.127] @30m — all significant; at real levels the same ICs are ≈0 to negative (−0.027 @5m). Reading: at an arbitrary price, short-term flow simply continues (generic momentum); AT REAL STRUCTURE that continuation is neutralized — real levels are where naive flow-following stops working. Mechanically consistent with F4's inversion (absorbed aggression at structure ≠ follow-through).
- **→ F5b PRE-REGISTERED (2026-07-07):** hypothesis "flow-neutralization at structure": IC_twin(d*/ct_vol, 5m) − IC_real(d*/ct_vol, 5m) > 0 — declared quantity, declared sign, test set = L2 screening days < 2026-06-16 (independent), same harness.
- **MULTIPLICITY RULE (frozen now):** the L2 confirmation pass will test ALL forward-registered hypotheses (F4b, F5b, and any later ones) under Benjamini-Hochberg FDR at q = 0.10 across the registered set — emergent hypotheses don't get free single-test α.
- **Decision:** F5 recorded (UNDERPOWERED; forward-queue); F5b registered. Next: F6 (approach imbalance — does how price ARRIVES matter?).

## 2026-07-07 · L2 screening set CERTIFIED · F4b/F5b confirmation pass — first survivor, one reclassification

- **L2 build complete (`cracker_p1_trace_l2.ts` → `cracker-trace-l2.db`):** NQ 19,908 visits / 44 days (05-05→07-06) · ES 12,682 / 40 days. One benign skip (06-06 depth partition missing). 06-29 included (CQG intact; the exclusion was BMD-only). Hold rates in the placebo band on a THIRD dataset (0.642–0.675, both symbols — the placebo wall is now triple-replicated).
- **P2 gates on L2: 2 PASS / 0 FAIL** (markout self-test + 100% label re-derivation). Random-time markout SDs ≈ L3 values (NQ 16.8/37.5/62.0/81.4pt vs 17.7/41/70/94) — the frozen RT_SD constants transfer as assumed. **L2 power table published → `docs/cracker-power-l2.md`** (NQ full-n MDE 5m 1.53pt / 15m 2.40pt — ~2.5× tighter than L3).
- **Confirmation pass (`cracker_p3_confirm_l2.ts`) — registered family {F4b, F5b}, independent NQ days < 06-16 (29d, 8,989 real visits), one-sided bootstrap p, BH-FDR q=0.10:**
  - **F5b (flow-neutralization at structure): ★ CONFIRMED.** twin−real IC(d*/ct_vol, 5m) = +0.040, one-sided p = 0.0558, BH-rejected (step-up). Declared sign ✓. Pattern detail: on L3 the twin was +0.078/real ≈0; on L2 twin ≈+0.008/real −0.032 — the DIFFERENCE (the registered quantity) reproduces, and the L2 shape sharpens the reading: at real structure flow-following mildly REVERSES (fading the aggressor at real levels), not merely neutralizes. Effect small (ΔIC ≈ 0.04); ES descriptive ≈ 0. **Cracker's first surviving hypothesis.** Final word still awaits the L3 forward lockbox at Phase 5.
  - **F4b (absorption → 1m continuation): formally BH-rejected (p = 0.0585, sign ✓) BUT FAILS ITS OWN REGISTERED TWIN GATE** — the registration said "twin required"; on L2 the placebo twin shows the SAME effect (twin −0.044 vs real −0.039). The inversion is real but NOT level-specific: high volume-per-penetration predicts 1m continuation at ANY price — a general microstructure/tape effect, not a level factor. **Reclassified: F4b = confirmed general-tape phenomenon; REJECTED as a level factor.** (Both-pools-combined evidence is stronger than either alone; candidate input for later composition phases as a tape-state variable, NOT a level-interaction factor.)
- **Honesty notes:** both p-values are marginal (0.056/0.059); F5b survives via BH step-up (its rank-1 threshold alone was 0.050). Effects are small. Nothing here is tradable standalone; the value is mechanism knowledge feeding Phase 6 composition + the Phase-5 lockbox test.
- **Decision:** L2 set live for the ladder. F5b = CONFIRMED (small); F4b reclassified general-tape. Ladder continues: F6 approach imbalance (screen on L2 at ~2.5× power + L3 in parallel).

## 2026-07-07 · P3 · F6 — approach imbalance: NULL (first powered nulls at L2 scale)

- **Spec (pre-registered):** a* = bounce-directed approach delta (60s attack phase, ring-buffer), raw + fraction; declared 1m/5m; no sign imposed (momentum-through vs exhaustion stories both live); L2 = screening verdict, L3 = sign-consistency check.
- **Result: NULL.** On the L2 screen (7,434–7,447 NQ visits/half — MDEs down to 0.054): NQ fraction cells are **POWERED NULLs** (1m: train −0.035/valid +0.008; 5m: +0.001/−0.025 — sign-inconsistent noise around zero); raw cells marginally underpowered with estimates ≈0. ES: all cells flat (±0.03) and underpowered. L3 parallel run: noise, sign-consistent with nothing. Twins ≈ 0.
- **Reading:** how price ARRIVES at a level — the net aggression of the attack — carries no measurable information about the resolution, at 6× the original power. Combined with F5 (flow AT the level ≈ nothing at real structure), the flow-direction family is now empirically dead as a standalone level signal on both phases of the interaction. What remains untested from the flow family: sweep GEOMETRY (F7 — penetration shape, not flow direction) and test-over-test flow DIVERGENCE (F8 — change across visits, not levels).
- **Decision:** F6 recorded NULL, dropped. Next: F7 sweep geometry.

## 2026-07-07 · Audit note — visit-count anatomy (user challenge) + registered band-sensitivity item

- **Challenge (Ravi):** "why are levels in the thousands?" **Audit:** they aren't — rows are VISITS. NQ L2: 633 real levels / 44 days (≈14 born/day — trader-plausible), ~240 active at once, ~23 visits per level lifetime, ~5 per in-play day, ~450 visits/day ⇒ 19.9k rows. Bookkeeping consistent.
- **The real finding of the audit:** the visit trigger band (= σ_1m, frozen interim) averaged **18.4pt** in this regime — wide; big days have price inside several zones at once (07-02: 1,275 visits / 280 levels). Defense: band < noise scale re-creates the touch-fragmentation disease the hysteresis fixed; overlap is handled by uniq_w + cluster_id + day-block bootstrap; and all verdicts are real-vs-placebo under identical rules — a wide band dilutes power symmetrically, it cannot fabricate an edge. Caveat acknowledged: neighborhood-pass-throughs may MASK effects that exist only at tight tests.
- **REGISTERED for the Phase-8 sensitivity sweep:** re-run ladder survivors (currently F5b) and headline nulls (F6) at band ∈ {0.5σ_1m, min(σ_1m, 8pt NQ / 2pt ES)} — pre-registered now, not run now (mid-ladder parameter changes after seeing results are the exact failure mode the protocol forbids).

## 2026-07-07 · P3 · F7 — sweep geometry: no edge; the stop-run-reversal folklore leans the WRONG WAY

- **Spec (pre-registered):** pool = swept-and-reclaimed visits (penetration > 0, held = 1, both known at close); factor = (penetration/band)/max(dwell_min, 10s) — deep-and-fast scores high; expected POSITIVE (violent sweep-reclaim traps stops → fuels bounce); declared 5m/15m; L2 screens, L3 sign-consistency.
- **Result: no EDGE; official labels UNDERPOWERED at all declared cells — but the structure is the story:**
  - **The 4th train mirage, now at scale:** L2-NQ 5m train +0.055 CI [0.004, 0.114] *significant positive* (the folklore, on 2,579 visits!) → validation **−0.082 CI [−0.140, −0.016] significant NEGATIVE**. Both halves significant, opposite signs → regime-unstable; per the frozen taxonomy the label is UNDERPOWERED (MDE 0.091), but the sign-flip means: dropped from the queue with NO forward expectation (F1 precedent).
  - **Everything out-of-train leans NEGATIVE:** L2-NQ valid −0.082 (sig), L3-NQ train −0.070 (sig) / valid −0.098, ES valid −0.096 (L2) / −0.140 (L3, sig). And the TWINS are negative too (L2 valid −0.125 sig) → like F4b, not level-specific.
  - **Reading:** deeper/faster spike-and-reclaim predicts LESS follow-through, not more — spike exhaustion/mean-reversion, generic to the tape, opposite to the stop-run-reversal story. The trader folklore appears to have the sign backwards.
- **→ F7b REGISTERED for the FORWARD family (2026-07-07):** "sweep-exhaustion (general tape): IC(sweep, y(5m)) < 0 on swept-and-reclaimed visits, level-agnostic (real+placebo pooled)." Test set = the L3 FORWARD LOCKBOX at Phase 5 (the L2 set is consumed — it screened this factor and cannot confirm its own child hypothesis). Forward family {F7b, …} gets its own BH-FDR at q=0.10, separate from the L2-confirmation family.
- **Decision:** F7 recorded, dropped as a standalone level factor. Ladder remainder: F8 (test-over-test delta divergence) — the last rung.

## 2026-07-07 · P3 · F8 — test-over-test delta divergence: NULL — Carmine's discriminator is flat at scale · THE F1–F8 LADDER IS COMPLETE

- **Spec (pre-registered):** pairs = consecutive same-side tests of the same level; att = aggression INTO the level as contact-volume fraction; factor = att_prev − att_cur (positive = weakening attack); expected positive (weakening attack → hold → bounce); declared 5m/15m.
- **Result: NULL.** With 6,415/6,560 paired re-tests per half on the L2-NQ screen, every estimate sits within ±0.037 of zero with no sign consistency: NQ-L2 15m POWERED NULL; L3-NQ both declared cells POWERED NULL; the rest underpowered-but-flat. Twins ≈ 0 (L3 twin mildly positive — generic). Whether the re-test arrives with weaker or stronger flow than last time tells you nothing about the outcome.
- **THE SINGLE-FACTOR LADDER (F1–F8) IS COMPLETE.** Final scoreboard:
  | F | factor | verdict |
  |---|---|---|
  | F1 | source identity | NULL (train mirage killed OOS) |
  | F2 | confluence | NULL (powered, near-miss theme queued) |
  | F3 | visit history / 94-7 | NULL (in-sample "memory" = day-regime artifact) |
  | F4 | absorption ratio | UNDERPOWERED → **F4b general-tape effect (confirmed, not level-specific)** |
  | F5 | contact delta | UNDERPOWERED → **F5b CONFIRMED: flow-following reverses at real structure** |
  | F6 | approach imbalance | POWERED NULL |
  | F7 | sweep geometry | no edge; folklore sign-backwards → F7b forward-registered |
  | F8 | test-over-test divergence | NULL (powered at key cells) |
- **What survived Phase 3:** one confirmed structural mechanism (F5b), one confirmed general-tape effect (F4b), one forward-registered tape hypothesis (F7b), a recurring sub-MDE "NQ 5m structure bounce" theme accruing forward, and four measured demolitions of core level-trading folklore. NO standalone tradable factor — exactly what the two-gate design anticipated: single features don't trade; compositions might.
- **Next per plan:** PHASE 4 — the heatmap/book-state engine (pulls, adds, icebergs, synthetic refills — the L3-only order-book detectors, never yet consumed by the trace), whose factors then join the ladder; per the rebuild directive this is also the trigger to REBUILD the order-book detector layer as a Cracker-owned primitive. Forward-queues keep accruing (~430 visits/day L3) toward the Phase-5 lockbox.

## 2026-07-07 · P4.2 · F9/F10 — the book-state factors: NULL — the heatmap folklore joins the graveyard

- **Rebuilds:** all four traces rebuilt with visit_book columns (L2 counts reproduced exactly; coverage 95–100%). Mid-session partial-day contamination caught and fixed: 685 partial 07-07 rows purged from L3; runners now refuse the current ET day even on full rebuilds (TRACE_INCLUDE_TODAY escape hatch); nightly refills 07-07 tomorrow.
- **F9 (wall ΔL over approach, pulled/added/defended — declared 1m/5m): NULL, POWERED, on the L2-NQ screen** (7,300+ visits/half; both declared cells MDE ≤ ρ*): ICs within ±0.017, sign-inconsistent; terciles flat; twins ≈ 0. ES/L3: underpowered noise. The "watch the wall get pulled" story carries NO bounce information.
- **F10 (capacity behind the level, trapdoor — declared 5m/15m): NULL at the powered cell** (L2-NQ 5m: train −0.034 n.s. / valid −0.026 sig — no same-sign train support; if anything a faint ANTI-folklore lean: more capacity behind ⇒ slightly worse bounce). 15m UNDERPOWERED; L3-NQ 15m produced the 5th train mirage (train −0.128 SIG → valid +0.056). ES: nothing. gap_max secondary: nothing.
- **Phase 4 conclusion:** the minimal heatmap adds no level-interaction signal. Combined with F1–F8: NOTHING observable about a level or its approach — identity, stacking, history, trade flow, OR resting liquidity — predicts the resolution at these horizons and power. The confirmed structure (F5b) remains the one mechanism: levels scramble flow signals. Remaining untested mechanism: F11 iceberg/replenishment (Phase 4b, L3-only, low-N mechanism-grade).
- **Decision:** F9/F10 recorded NULL, dropped. Phase 4 minimal scope complete.

## 2026-07-07 · P4b · F11 — iceberg/replenishment: NULL at declared cells — and the mechanism-grade finding is that TRUE icebergs barely exist at visits

- **Health gate:** all 12 days both symbols 100% two-sided → **the P0.3 open item (days that couldn't sustain a valid L3 book) is CLOSED — it was the capture-order corruption, cured by the repair.**
- **Mechanism-grade descriptives (the real finding):**
  - **True icebergs (filled beyond max displayed / replaced-up, still alive at contact end) are present at only 0.3% of NQ visits (12 of 3,937; median hidden-filled 5 lots) and 1.3% of ES visits.** Whatever traders think they see as "iceberg defense" at levels, the strict order-lifecycle signature is a RARE event, not a regular mechanism — F11a is unusable as a signal for lack of occurrences, independent of any IC.
  - Synthetic replenishment (fill→repost ≤1.5s) is the opposite: present at 75%/83% of visits — ubiquitous algo requoting, i.e. mostly baseline noise, not a discriminator.
  - Book-character note: during visits, pulled>added on 54.8% of NQ visits vs only 8.4% on ES — the ES book is structurally add-dominant (descriptive; explains some micro/mini family differences).
- **Verdicts (declared 5m/15m):** F11a: UNDERPOWERED/NULL both symbols (no variation to rank). F11b: NQ 5m **NULL (powered)**; NQ 15m underpowered ≈0; ES 5m underpowered (train −0.083 sig, valid n.s.); ES 15m NULL. **No EDGE.**
- **Emergent (undeclared 1m, ES only): F11b train −0.062 CI [−0.100, −0.004] AND valid −0.060 CI [−0.118, −0.012] — same sign, both significant, twin n.s. (−0.020).** Would meet EDGE at an undeclared horizon → per the frozen rule, registered instead: **F11c** = "replenishment intensity at the defending zone predicts 1m continuation INTO the level (negative IC), ES" — mechanically consistent with the F4b absorption-continuation family (defense that has to keep refilling is a level under real attack). Forward-lockbox family, BH q=0.10.
- **THE DISCOVERY PHASE IS COMPLETE.** All four mechanism families measured: structure (F1–F3 null), trade flow (F4–F8: null at levels; F5b confirmed structure-scrambles-flow; F4b/F7b general-tape), displayed book (F9/F10 null), hidden liquidity (F11: true icebergs rare; refills noise at declared horizons; F11c registered). Open forward family: {F5b-fwd, F7b, NQ5M-THEME, F11c}. Next phase of work = forward maturation → BH resolution → Phase 5 composition if the survivor set justifies it.
